# List of disabled recipes (specify their namespaced keys, e.g., 'minecraft:iron_pickaxe')
disabled: []

# Manually created groups for batch enable/disable.
# A group will only appear in the GUI if it is listed here.
groups:
  test_group:
    - minecraft:iron_pickaxe
    - minecraft:iron_sword
    - minecraft:iron_axe
